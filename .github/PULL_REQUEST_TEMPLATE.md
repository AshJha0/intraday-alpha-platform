<!-- Title: "<area>: <what changes>" — link the issue with "Closes #N" or "Part of #N". -->

## What and why

<!-- One paragraph. Name the contract clause (PLATFORM_CONVENTIONS.md §, API_*.md §,
     schemas/<domain>/<file>.schema.json) this serves. -->

Closes #

## Language(s) touched

- [ ] python (`python/src/iap`, reference)
- [ ] cpp (`cpp/`)
- [ ] rust (`rust/`)
- [ ] java (`java/`)
- [ ] docs / configs / schemas only

## Golden vectors

- [ ] No golden file changed.
- [ ] Golden file(s) regenerated: `tests/golden/…`
  - by which reference tool: `python/tools/make_golden*.py` / `cpp/tools/make_replay_fills_golden` / `rust/risk/src/bin/make_risk_golden` (name it)
  - validated first against the independent brute-force implementation (`python/tests/bruteforce_*.py` or the port's brute test)
  - every other language re-matched (parity table below)
  - `schemas/MIGRATIONS.md` entry added

## Contracts and versions

- [ ] No schema, config or checkpoint document changed.
- [ ] `x-version` bumped on: `…` + `schemas/MIGRATIONS.md` entry (what changed, why, migration path).
- [ ] A file moved within `schemas/` or `configs/`: old -> new path table added to MIGRATIONS.md (no bump).

## Determinism

- [ ] No wall clock, no unordered-map iteration, no unseeded randomness on any deterministic path this PR touches (PLATFORM_CONVENTIONS.md §3). Randomness, if any, is SplitMix64 seeded from `configs/`.
- [ ] Integer `price_ticks` / `qty` / ns timestamps on every contract touched; no float crept onto a contract or hot path.
- [ ] Fail-closed behaviour preserved (unknown or degraded state produces no signal and no order).

## Research / lifecycle (delete if not applicable)

- [ ] Every new look at the data is in `research/experiments.json`; the report prints the denominator.
- [ ] A changed verdict (`research/alpha_reports/*.json`) or lifecycle state (`research/lifecycle_log.jsonl`) cites its ledger entry id: `…`
- [ ] Reports regenerated by their `run_*.py`, not edited by hand.

## Parity table

<!-- Paste the table from `bash tests/harness/run_all.sh` (or `--golden-only` for a docs-only PR that
     touches no code — say so). Counts are truthful: "-" or "?" fails the run. -->

```
language | tests passed | golden passed  | time   | status
---------+--------------+----------------+--------+-------
python   |              |                |        |
cpp      |              |                |        |
rust     |              |                |        |
java     |              |                |        |
integration |           | -              |        |
replay   |              | -              |        |
```

## Test counts vs README

- [ ] The counts above match the parity table in `README.md` (and `docs/ARCHITECTURE.md` §6), or this PR updates them.
- [ ] `python3 tests/harness/check_headline_numbers.py` exits 0 (or the research rows are refreshed).
- [ ] New tests are counted in the right level of `tests/README.md` (unit / golden / replay / integration / research validation / deployment).

## Docs

- [ ] The contract document for the area is updated (`API_*.md`, `PLATFORM_CONVENTIONS.md` §11/§12, `docs/SCENARIOS.md` for a new pinned behaviour, runbooks for an operator-visible change).
- [ ] `tools/github/issues.yaml` status/evidence updated for the issue this closes and `docs/EPICS.md` regenerated (`python3 tools/github/create_issues.py --render-md docs/EPICS.md`).

## Style

- [ ] PLATFORM_CONVENTIONS.md §8: type hints + docstrings (Python); `-Wall -Wextra -Werror` clean (C++17); `Result<_, IapError>`, zero warnings, no panics on input (Rust); `-Xlint:all` clean (Java 21); no dead code, no TODOs.
