[package]
name = "alpha"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "IAP production alpha scoring: the 6 golden alphas per API_ALPHA.md (linear_z_v1 + FX05 currency-exposure machinery)"

[dependencies]
marketdata = { path = "../marketdata" }
features = { path = "../features" }
serde_json = { workspace = true }
