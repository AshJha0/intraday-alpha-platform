[package]
name = "lifecycle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "IAP alpha promotion lifecycle: states, evidence, gate table, 17-edge transition machine (live rolling-IC rules), byte-identical alpha registry"

[dependencies]
marketdata = { path = "../marketdata" }
contracts = { path = "../contracts" }
serde = { workspace = true }
serde_json = { workspace = true }
